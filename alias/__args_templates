@echo off

setlocal enabledelayedexpansion

set "args="
for %%a in (%*) do (
    set "arg=%%a"
    set "args=!args! '!arg!'"
)

where pwsh >nul 2>&1
if %errorlevel% neq 0 (
    echo PowerShell not installed, using cmd to run...
    ############# 这里写cmd的代码
) else (
   pwsh -c "#这里是命令 !args!"
)